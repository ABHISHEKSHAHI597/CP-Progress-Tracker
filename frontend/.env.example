# The app calls its own origin. Vite proxies /api to the local backend in
# development, and the Vercel rewrite forwards it to Railway in production.
# Set this to the same value in the Vercel dashboard.

VITE_API_URL=/api
